@model IEnumerable<Work_01.Models.Category>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3>Categories</h3>
    </div>
    <div class="list-group">
        @foreach (var cat in Model)
        {
            @*<a class="list-group-item" href="@Url.Action("CatNav","Home",new { id=cat.CategoryId})">
                @cat.CategoryName
                <span class="badge bottom-right">@cat.Products.Count</span>
            </a>*@

            <a class="list-group-item" href="@Url.Action("Product","Home",new { id=cat.CategoryId})">
                @cat.CategoryName
                <span class="badge bottom-right">@cat.Products.Count</span>
            </a>
        }
    </div>
</div>